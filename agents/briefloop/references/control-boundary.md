# control-boundary

Use the public external CLI; do not mutate SQLite or fabricate task state. Submit uses the workspace model; revise saves the caller's edits. Preserve existing task authorization, exact versions, source IDs and credentials. See https://briefloop.ai/agents/briefloop/SKILL.md .
