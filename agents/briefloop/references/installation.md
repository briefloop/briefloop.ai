# installation

Install with `python -m pip install --upgrade briefloop`. Python 3.11+ is required. Open the workspace and configure its runtime in BriefLoop. Then use `briefloop external --workspace "/absolute/workspace" discover`. See https://briefloop.ai/docs/install-update.html .
