# briefloop.ai 网站仓库

- 视觉体系以 `DESIGN.md`（Audit Ledger 语言）为准；改样式前先读它，改动后跑 `npx @google/design.md lint DESIGN.md`。
- 完成视觉/页面类任务后，主动起本地预览服务器（如 `python3 -m http.server 8137`）并用 `open` 在浏览器打开给用户看，不要只给截图或文字描述。
